apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-multitool-deployment
  labels:
    app: nginx-multitool
spec:
  replicas: 3
  selector:
    matchLabels:
      app: nginx-multitool
  template:
    metadata:
      labels:
        app: nginx-multitool
    spec:
      containers:
      - name: nginx
        image: nginx:latest
        ports:
        - containerPort: 80
        resources:
          limits:
            memory: "256Mi"
            cpu: "500m"
      - name: multitool
        image: wbitt/network-multitool:latest
        ports:
        - containerPort: 8081
        env:
        - name: HTTP_PORT
          value: "8081"
        resources:
          limits:
            memory: "256Mi"
            cpu: "500m"